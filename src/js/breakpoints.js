const breakpoints = {
  getSM: (fontSize) => fontSize * 30, // 480px
  getMD: (fontSize) => fontSize * 48, // 768px
  getLG: (fontSize) => fontSize * 62, // 992px
  getXL: (fontSize) => fontSize * 75, // 1200px
  getXXL: (fontSize) => fontSize * 87.5, // 1400px
};

export default breakpoints;
